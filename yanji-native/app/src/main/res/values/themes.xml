<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Yanji" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 来电页是深色底，状态栏图标得反过来用白的，不然锁屏亮起时上面一排字看不见 -->
    <style name="Theme.Yanji.Call" parent="Theme.Yanji">
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowBackground">@drawable/call_bg</item>
    </style>
</resources>
